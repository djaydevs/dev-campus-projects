/* Programmed by Danny Jay M. Flores
   IT 1B
   Activity 1 Lesson 2
   Dec. 1, 2020
*/

#include <conio.h>
#include <stdio.h>

int main ()
{  
   int x=2,y=3,z=4;
   x=x+1;
   y=y*z;
   x++;
   
   printf ("\n The equivalent value of x is %d", x);
   printf ("\n The equivalent value of y is %d", y);
   printf ("\n The equivalent value of z is %d", z);

   return 0;
}